# MaxNow 前端样式上下文

这个文件记录 Dash 和 Blog 的视觉约定。以后调整页面样式前先读这里，避免把已经确认过的视觉方向回退掉。

## 总体气质

- MaxNow 是私人状态工作站和公开笔记发布层，不做营销页气质。
- 界面要平静、紧凑、可扫读，优先服务每天反复打开时的效率。
- 当前视觉基准是轻量用户中心风格：白色 sidebar、浅蓝灰背景、柔和白卡片、淡边框、克制阴影、彩色小图标入口和系统字体。
- 不要回退到深色重阴影、巨大 hero、装饰性渐变球、外部 Web Font 或过度插画化背景。

## 页面版式协议

- Dash / Blog 新页面、新模块和大幅调整必须先选用既有页面骨架，再写局部组件样式；不要在某个模块里临时发明一套孤立布局。
- 页面主内容优先使用统一 board / lane / grid，而不是多层自由嵌套列。模块之间的组合关系应由外层 board 决定，单个模块只负责自己的内容排版。
- 同一页面里的卡片必须先声明卡型，再进入网格。Home 当前卡型为：
  - `wide-short`：横向短卡，适合 Token 热力格、AI 前沿简报、版本更新。
  - `wide-tall`：横向高卡，适合近期待办这类需要 2 行以上内容的列表。
  - `mid-short`：中等短卡，适合待推进。
  - `mid-tall`：中等高卡，适合市场涨幅这种固定条目列表。
  - `widget-compact`：右侧半宽小组件，适合今日 Todo、近期用量这类短扫读入口。
  - `widget-wide`：右侧满宽组件，适合豆奶摘要、系统状态这类内部还有指标网格或列表的短扫读状态模块。
- Home 主内容采用 `home-board` 两列外壳：左列是 `home-lane-primary`，右列是无视觉样式的 `home-side-stack`。左列必须承载个人任务和内容型长模块，包括 Token 热力格、Personal Wiki、待推进、AI 前沿和版本更新；版本更新固定放在 AI 前沿下方。右列只承载短扫读状态和入口，包括市场、今日 Todo、近期用量、豆奶、芭蕾和系统状态；芭蕾摘要属于 `home-lane-rail` / `widget-wide`，放在豆奶后、系统状态前。右列里的 `home-lane-signal` 和 `home-lane-rail` 只作为语义分组，视觉上展平成同一条 widget 网格：默认两列，`widget-compact` 占 1 格，`widget-wide` / `mid-*` 占 2 格，1320px 以下退回单列。新增 Home 模块必须先选择语义 lane、保留 `home-card-*` 语义类，并设置 `data-card-size`；不要再用固定 `grid-area` 把不同高度的卡片绑进同一张跨行网格，也不要为了二列或三列对齐硬拉大所有卡片。内容型长模块不能放进右侧一路下排造成左列空白。
- 卡片内容必须服从卡型：Home 列表卡只展示少量高信号条目，长标题用 line clamp，更多内容进入详情页或外链；不能让内容数量自由撑高首页。
- 每个页面只能有一个主版式系统。需要跨列、右侧 rail 或底部宽卡时，都在这个主系统里声明；短卡和高卡不要共享同一行高，不允许通过局部 margin、空 div、固定高度或嵌套卡片补白。
- 任何新增或移动的页面卡片，都要同步覆盖 hover / focus、语义色、响应式断点、缓存版本和文档规则；如果不能说明它继承哪一类卡型，就不要合入。

## 品牌区

- MaxNow 正式图标是 Owner 在 2026-06-21 确认的深蓝 `M/N` 标识。
- Dash 图标资产：`dash/assets/maxnow-icon.png`。
- Blog 图标资产：`blog/assets/maxnow-icon.png`。
- 左侧品牌区使用 28px 图标，无底框，只显示 `MaxNow`。
- Blog 品牌区不要显示 `blog.maxnow.cn` 副标题；Dash 和 Blog 对齐。
- 不要回退到旧的浅蓝 `M` SVG，也不要给 Blog 图标加蓝色底框。

## 圆角和卡片

- Dash / Blog / Blog Preview 的页卡圆角基准是 `14px`。
- 导航小图标容器圆角基准是 `12px`。
- 主要页卡包括：Home 状态卡、panel、Blog article card、Blog topic card、同行记地图卡、同行记统计卡、豆奶顶部 tab、豆奶图表卡。
- 页卡内部的小信息块通常优先继承 `var(--radius)`；豆奶指标使用图一参考风格：独立白底小卡片、淡边框、轻阴影，内容居中。
- pill 标签使用 `999px` 圆角，例如状态标签、日期标签、单位角标。
- 不要把卡片套卡片做成厚重层级；重复项、工具面板、小指标块可以用卡片，页面大区块保持轻量。
- Dash 所有 tab 的卡片禁止使用顶部彩色横条或渐变强调线；页面和模块语义色只放在标题、数值、图标、状态点、pill、边框反馈和轻背景中。

## 悬浮交互

- 桌面端可 hover 的页卡需要有轻微悬浮感。
- 当前标准：边框颜色变为语义色或浅蓝、阴影增强、`transform: translateY(-2px)`。
- Dash 模块 hover 统一使用 `--card-color` / `--card-border`：大模块按自身主题色，内部条目优先按自身 `data-tone`，没有独立语义时继承父模块主题色。
- 豆奶顶部三个 tab 也要保持 hover 悬浮，不要只让内部小指标悬浮。
- 新增或移动卡片 / widget 时，先判断它属于哪一组现有组件，再把同组的 hover / focus / transition 规则一起套上；不能只补静态布局。
- 触摸端不要强行依赖 hover 效果；响应式布局首先保证不挤出、不遮挡。
- Home 豆奶摘要卡作为可点击入口，应保持整体 hover / focus 可感知。

## 颜色使用

- 不要让页面变成单一蓝色系。蓝色是主色，但状态和资源指标需要多元语义色。
- 当前语义色：
  - 蓝：主信息、流量、默认强调。
  - 橙：到期、推进、需要注意但不是错误。
  - 青：日均、时长、辅助资源。
  - 紫：累计、Token、计数。
  - 绿：健康、正常、累计延长。
  - 红：错误、失败、危险状态。
- Home 状态卡应保持多色区分：Token 活动蓝、今日推进橙、Token 摘要紫、数据健康绿。
- 同行记页面用粉色作为 Ricky / 共同记录的主语义色，但统计卡仍要混合蓝、橙、青等颜色，避免整页变成单一粉色。
- 规划中的芭蕾页面使用独立的粉玫瑰 + 白色语义：可读主色建议 `#b94f78` / `#c04f7c`，浅背景 `#fff3f7`，淡边框 `#efbfd0`。它不能直接复用同行记的高饱和亮粉；粉玫瑰只进入标题、图标、pill、关键数值、hover 边框和 3%-6% 的混白渐变，页面仍保留 MaxNow 的浅蓝灰背景与白卡。
- 芭蕾页内部继续使用多色状态：浅冰蓝表示课程 / 时间信息，绿色表示正式预约 / 完成 / 目标达成，橙色表示候补或临近取消截止，紫色表示累计课时，红色表示授权失效或同步失败；浅粉不能承担正文文字。
- 豆奶顶部参数固定为：
  - 剩余流量：蓝
  - VIP 到期：橙
  - 日均可用：青
  - 累计签到：紫
  - 累计流量：蓝
  - 累计延长：绿
- 豆奶顶部参数要保留小 SVG 图标和对应语义色，不要回退到纯文字参数块或全蓝 / 全青。
- 豆奶指标的结构固定为：独立小卡片内左侧小图标 + 右侧标题同一行，数值放在下一行；整体内容居中，图标和数值使用同一个语义色。

## Home 小日历

- Home 顶部右侧的时间卡是紧凑小日历，不只是时钟。
- 固定展示公历日期、当前时间、农历日期、当天节日 / 个人特殊日期，以及严格晚于今天的下一特殊日期。
- 小日历本身要像一个明确的信息块，内容居中；时间是最大视觉层级，农历和节日不要挤成过小的角落文字。
- 节日提示保持轻量，使用橙色强调；没有节日时显示弱提示，不要扩大成额外卡片。
- 下一特殊日期单独占一行，使用紫色轻量强调和“x天后是xx日（x月x日）”格式；当天提示与下一日期必须同时保留，文本可换行但不能撑破卡片或造成横向溢出。

## Home 今日状态卡

- Home 顶部 Today Status 卡以自动推导为主：今日 Todo 优先，其次按自动化异常、当前时段、ROADMAP 待推进 / 主线、Token 活跃生成“执行 / 巡检 / 复盘 / 推进 / 探索”等模式。
- `dashboard.json.today` 只作为当天人工 override；只有 `today.updatedAt` 命中浏览器当天日期时，才显示“今日确认”并采用手动模式 / 摘要 / 焦点。旧日期判断不再显示成过期主状态。
- 顶部 pill 表示状态来源：当天人工确认为绿色，自动生成通常为青色，需要行动的 TODO / ROADMAP 为橙色，自动化异常或缺信号为红色。
- 状态卡内部的信号区不做卡片套卡片，也不使用顶部彩色横条；时间进度使用固定青色的 24 小时圆形进度环：环内显示今天已过去的整数百分比，当前时间用独立 pill 显示在圆环下方。`自动生成` 新鲜度 pill 必须紧邻左侧 `Today Status` eyebrow，不能被推到圆环旁边。1501px 以上使用等宽左右区域夹住 140px 圆环列，让圆环中心与状态卡内容区中心严格重合，右侧四条信号独占一列；视口不超过 1500px 时，天气与日历整组换到状态卡下方，状态卡恢复紧凑两列，不能靠压窄状态文案硬塞。圆环不随机取色，也不承担异常 / 推进 / Token 语义色；这些语义色只用于右侧对应文字和节点。每个信号节点必须作为第一行的网格项，与标签和主值处于同一行中心，不能按包含副说明的整条两行内容居中。
- 顶部状态条四张小卡固定回答：今日执行、数据同步、Token 7 天、系统自动化；不要再把当前主线 / 待推进这类已在 Today Status 或下方模块出现的信息做成重复数字卡。
- Home 待推进卡右上角使用现有 `status-pill` 展示 `project-status.*` 新鲜度；正常为蓝色“路线图已同步”，超过阈值为橙色“待刷新”。过期项目状态可以保留在卡片中供核对，但不得继续驱动 Today Status 推荐。

## Home 今日 Todo

- Home 右侧 Today Todo 卡替代旧的静态时间点模块，只展示 personal-wiki 中 `due_at` 等于浏览器当天日期的未完成待办；过期未完成和无日期待办不放进这里。
- Today Todo 使用和 Personal Wiki 近期待办一致的轻量条目风格，右侧保持单列，不增加额外卡片嵌套。

## Home 天气卡

- Home 顶部天气卡放在小日历左侧，用来展示北京市海淀区天气；它和小日历是独立小组件，不放进 Today Status / 整理模式卡片内部。
- 天气卡和小日历属于 Home 顶部右侧同一组 widget：桌面端必须与左侧 Today Status 卡片同顶、同底、等高，并保持同一组 hover / focus 悬浮反馈。
- 天气卡必须有图标：晴天用太阳，多云 / 阴用云，下雨用雨，雷阵雨用闪电雨，雪用雪，雾霾 / 雾用雾线。
- 天气卡视觉层级要和小日历对齐：第一排只放最大的天气图标，第二排放当前温度，第三排把地点、天气状态和低温 / 高温放在同一行。
- 天气图标使用轻量 SVG，作为地点行里的辅助标识；颜色按天气语义变化，但不要做成大面积图标容器或装饰插画。
- 天气卡显示地点、当前温度、天气状态、今日高低温和短更新时间；更新时间使用 `HH:mm 更新` 的弱文本，不抢主天气信息。
- 窄屏下天气卡和小日历纵向堆叠，不能横向溢出。

## 非首页页面协议

- 豆奶、Token、芭蕾、云服务、生活和同行记统一给视图添加 `secondary-view`；只有豆奶保留承载业务指标的 `secondary-page-head` 三页头。Token、芭蕾、生活和同行记的标题、更新时间与同步状态进入全局顶部栏，内容区不重复页面标题卡；云服务首张内容卡固定为“系统与托管”；Home 不使用这组 class。
- 当前六个详情页面共用白底、轻主题色渐变、`var(--radius)` 圆角、统一阴影以及现有 hover / focus 上浮反馈；芭蕾页使用“本周训练 / 课程卡 / 成长进度”三卡概览作为入口。所有页面不使用顶部彩色主题线。豆奶三页头、普通 `panel`、Token 来源状态条、Token 摘要卡和同行记统计卡都继承这套干净外壳。
- 共享 hover 反馈只增强语义边框、阴影并上浮 `2px`，必须保留组件静止态的白底或轻主题色渐变；禁止统一覆盖成纯白。这样原本白底的列表仍保持白色，粉白、紫白、青白等语义卡也不会在鼠标移入时丢失底色。
- 当前页面语义色固定为：Token 紫色、豆奶青色、芭蕾粉玫瑰、云服务蓝色、生活绿色、同行记亮粉。页面内部仍可用蓝、紫、青、橙、绿区分指标和模块，不把整页染成单色。
- 全局顶部栏中的页面名、更新时间与同步状态在桌面同排，窄屏允许自然换行；豆奶三页头继续按内容等高。`860px` 以下详情页内容全部自然堆叠且占满可用宽度，不允许产生整页横向滚动。
- 统一只作用于视觉外壳和状态层级，不改 Token 范围、豆奶统计、云服务运维说明、吃啥交互或同行记地图的数据契约。

## 芭蕾展示

- 左侧导航名称为“芭蕾”，副标题为“课程与进度”，位置固定在 Token 与云服务之间；图标使用简化足尖鞋 / 舞鞋，不用心形，避免和同行记混淆。
- 顶部栏已经显示“芭蕾”，内容区不再重复标题卡，也不为正常同步状态保留空白行。低权重短更新时间与薄连接状态直接放在顶部栏标题旁；宽屏保持同一行，窄屏可在标题组内自然换行。内容区第一项应直接是异常告警（仅异常时出现）或三张概览卡。不得显示 Cookie、Session 指纹、systemd 单元或日志路径。
- 第一屏使用“本周训练 / 课程卡 / 成长进度”概览：`1501px` 以上三张卡各占三等分栅格一格并保持同顶、同底、等高；`1101px–1500px` 前两张各占一半，成长卡独占下一行并把升班 / XP 两块并排；`1100px` 以下三张卡单列，`860px` 以下成长卡内部也单列。本周训练使用 2 × 2 指标，课程卡纵向收纳卡信息、两项指标和计划结论，成长卡用蓝 / 紫轻底区分升班课次与 XP；`560px` 以下指标单列。三张外层面板继承 `panel` 家族圆角、边框、轻阴影、hover / focus、transition 和粉玫瑰语义色，成长卡内部块继承同族 hover / transition，不新增高饱和彩条。
- “课程计划”合并正式预约 / 候补与周日待抢目标：桌面两组并排，`860px` 以下单列。预约列表首行使用粉白整行高亮并增加“最近一节”实心标记，取代独立的下一节面板；预约行保留日期、星期、起止时间、课程、老师、教室、取消截止和状态。待抢行展示日期、时间、课程、老师、教室与逐课结果。运行时刻、重试、Session 和内部状态不进入此面板。
- 本周课程表放在芭蕾页最底部并沿用 `panel` 外壳，桌面始终使用内容区全宽；横轴先按周内日期分组，每天固定分为“大教室 / 小教室”两列，纵轴为时间，使用白底、淡网格和低饱和课程卡形成标准周日历。课程必须落入对应教室列；同一教室的重叠课程只在该列继续分轨，不能挤占另一间教室。整点文字压在对应横向分界线上，并与网格共用分钟坐标；最后一个时段必须补结束整点标签，不能只显示开始整点。课程卡必须按实际起止分钟纵向定位与伸展，例如 `11:00–12:30` 覆盖一个半小时。整周没有任何课程实际占用的连续小时收为一个窄的斜纹 `xx:00–xx:00` 跳过行；课程跨越的小时不得压缩。当天使用轻粉列和玫瑰标记，当前时间只用一条细玫瑰线，不再使用大面积深粉表头。桌面全部时间段和 7 天课程必须在面板内直接平铺，不设置内部固定高度、sticky 表头或横纵滚动；面板随课程内容自然增高且不得撑宽整页。课表在 `1200px` 以下切换为逐日、再按教室分组并自然展开，同样不得限高滚动。普通课程保留课型单一纯色等宽描边，不叠加左侧色条；课程状态额外使用高对比实心标签、轻状态底色与标题区同色图例区分“可约”绿色、“可排队”橙褐色、“已满”深灰紫和“已取消”红色。已预约为粉玫瑰实心、已上完为绿色实心、排队中为橙色实心，三种本人状态优先覆盖普通课程状态色。过去未上课程可以降饱和，但已上完课程保持实心状态。
- 课表分界线采用自然的同宽色阶：日期边界和同日教室边界都是 `1px`，日期线用稍深暖灰粉、教室线用接近背景的浅灰粉。避免贯穿全高的粗玫瑰线，也不要让两级线条完全同色。
- “训练记录”把统计与历史合进一张全宽面板，通过“本月 / 今年 / 全部”和“节数 / 时间”切换范围与指标。主数字、课程类型和课程级别同步变化；分类使用水平条。所选范围少于 5 节时隐藏大趋势图并显示短说明，达到 5 节后才展示折线趋势。历史紧随统计，不再另占一张外层卡。
- 课程卡放在顶部本周训练右侧并保持等宽；成长进度放在宽屏第三格。卡名 / 有效期、课程使用 / 有效进度和计划结论按当前卡宽纵向布局。剩余与已用合为同一事实组，避免四张等权指标重复表达。开卡未满 28 天时不渲染实际节奏结论。成长进度数字使用等宽数字，进度条只表达比例，不用颜色暗示老师已经批准升班。
- 自动抢课运维与 Session 实验详情放在 Cloud：宽桌面两张卡按自动抢课在左、Session 在右各占半宽，Session 展开后也不得横跨整行；`1320px` 以下随 Cloud 网格退回单列。自动抢课卡展示下次 / 上次执行、累计成功、固定优先级、重试与失败边界；Session 使用原生 `details / summary` 并默认展开，Owner 仍可手动收起，桌面四列指标、`860px` 下两列、窄屏单列。Session 不展示计划结束日期；无限运行是调度语义，身份失效或连续异常仍安全停止。失败状态冻结证据并使用红 / 橙语义；文案使用“自动检查”，不能写成“自动续期”。
- 手工补录课程在历史标签区使用低权重“手动添加”pill，与课程类型、级别和时长并列，不制造新的高饱和状态色。
- 历史列表保留日期、课程、老师和时长；桌面历史行使用统一的响应式日期轨道，课程信息列的起点不能因右侧标签数量或宽度不同而左右漂移；`860px` 以下再按既有两列 / 单列规则自然重排。学习目标 / 笔记只读 personal-wiki。
- Home 芭蕾摘要只展示一节下一课程、本周进度和同步状态，不复制历史、可约课程、图表或技术状态。同步失败时保留最后成功内容并明确标旧，不能把失败空态画成“暂无课程”。
- 页面整体不能变成芭蕾宣传站：不使用舞者插画、外部衬线字体、大面积高饱和粉色或宣传图式 hero。粉白是语义层，不是另一套品牌；本周课表使用白底淡网格、轻粉今天列和细玫瑰当前时间线，避免整块深粉表头。课程类型与状态仍只各保留一套颜色逻辑，预约 / 排队 / 已上完的实心状态必须优先于课型色。
- 第七个导航入口已经验证桌面 `720px` / `768px` 高度下侧栏可滚动，并验证 `860px` 紧凑布局与 `390px` 单列布局没有孤立错位或横向溢出。

## 豆奶展示

- 豆奶详情页顶部拆成三个并列 tab：标题、账号余量、签到累计。
- 三个 tab 在桌面端并排，窄屏下堆叠，不能横向溢出。
- 豆奶详情页顶部参数使用紧凑单位：
  - 天数使用 `d`，例如 `55d`。
  - 小时使用 `h`，例如 `1.50h`。
  - 天 + 小时使用 `3d 16h`。
  - 每日单位使用 `/d`，例如 `4.2 GB/d`。
- 图表单位角标使用 `GB/d`、`MB/d`、`h/d`。
- Home 豆奶摘要固定两行三列：
  - 第一排：今日流量、今日豆丁、今日延长。
  - 第二排：累计签到、累计流量、累计延长。
- Home 豆奶摘要同样使用“icon + 标题 / 数值”结构，每个指标独立成白底小卡片，内容居中。
- Home 只放豆奶摘要入口，不放趋势图；趋势图属于豆奶详情页。

## 图表和宽度

- 折线图容器要铺满卡片内部宽度。
- 图表可以保留最小渲染宽度以保证刻度可读，但容器不能让页面横向溢出。
- 先检查 `scrollWidth > clientWidth`，再确认是否是图表内部横向滚动，而不是整页溢出。
- 图表标题保留中文说明，单位角标可以用紧凑英文单位。
- 芭蕾趋势图切换月份、年份和全部范围后必须同步更新标题、X 轴粒度、单位和无数据状态；缺失月份补零，不能把同步失败误画成零值折线。

## 同行记展示

- 左侧导航名称为“同行记”，副标题为“我和 Ricky”，避免把它做成泛旅行产品。
- 同行记的页面名、更新时间与同步状态进入全局顶部栏，内容区直接从统计和 `panel` 地图开始；地点和旅行记录列表先不展示。
- 同行记地图优先使用 Leaflet + OpenStreetMap/CARTO Voyager 真实地图，点位来自 `dash/data/ricky.json` 的 `lat` / `lng`；地图容器需要铺满地图卡横向宽度，并使用稳定的响应式高度，marker 文案使用显式 `mapLabel`，不要用地点名前两个字自动截断；视觉要偏柔和、卡通、手账感，不要像严肃 GIS 后台；内置浅色 SVG 只作为地图脚本或网络不可用时的 fallback。
- 没有真实旅行记录时显示克制空状态，不编造地点或经历。
- 地图卡和右侧地点 / 记录卡在桌面端并列，窄屏下堆叠；页面不得横向溢出。
- 真实地图 marker 使用彩色圆润自定义 marker 和 Leaflet popup 展示地点摘要；fallback 地图默认只显示点位，hover / focus 时显示标签。

## 生活展示

- 左侧导航名称为“生活”，副标题当前为“吃啥”；它是轻量生活工具页，不要做成泛生活信息流或营销页。
- 生活的页面名、更新时间与同步状态进入全局顶部栏，内容区直接从 `panel`“吃啥”工具开始并默认可操作。
- “吃啥”应像一个轻量抽签台，不要回退成普通表单：结果舞台放在功能卡核心位置，候选项用可勾选菜品筹码，数量用小步进器，主按钮“吃啥”视觉权重要高于全选 / 清空。
- 点击“吃啥”时使用卡片内部滚轮动画，速度曲线从快到慢，最后停在最终菜名；数量为多个时应生成上下叠放的多个独立滚轮，每个滚轮只显示一个当前菜名，避免露出长轨道或两个菜名连成一串。选项筹码和滚轮项应使用一致的菜品 tone，不制造页面横向溢出，不影响最终可读性。
- 生活页主语义色使用绿色，但候选项可以混合青、橙、绿等 tone，避免整页变成单一绿色。

## Token 展示

- Token 的页面名、总账更新时间与同步状态进入全局顶部栏；范围切换 `1d / 7d / 30d / all` 保持顶部栏右侧紧凑选项，窄屏下才换行铺满。
- 内容区不再保留只承载标题与更新时间的大页头，直接从七项用量摘要开始。
- 分来源更新时间保留为用量摘要后的紧凑状态条；桌面四列平铺，`860px` 以下切为单列，不与页面标题争夺顶部空间。
- Home 原“当前主线”位置使用近 90 天每日 Token 活动热力格，格子应横向铺满卡片宽度，不出现横向滚动条，不保留过多底部空白，悬浮展示日期和 token 数。
- Token 页消耗趋势继续使用最近 30 天折线图，适合查看日级峰值和连续变化。

## Home 市场涨幅

- Home 主内容区使用统一 `home-board` 两列版式，不再使用单张跨行 grid-area 网格，也不回到随手新增局部左右列的拼法。
- 市场涨幅卡是 `mid-tall` 卡型，固定放在 `home-lane-signal`；不要用局部容器把它和某一张卡强行绑成一行，也不要把它挤进独立窄列。
- 市场涨幅卡沿用 `panel` 卡片族，内部行情条目沿用轻量白底小项、淡边框、hover 上浮，不做深色交易终端风格。
- 行情条目固定为左侧名称 / 代码、中间迷你走势、右侧涨跌幅 / 点位；红色表示上涨，绿色表示下跌，灰色表示持平或缺数据。
- 迷你走势由本地缓存的走势点渲染成 SVG，前端不直接请求第三方行情接口，也不嵌入外部网页或截图。
- 窄屏下 Token 热力格、Personal Wiki 近期待办和市场涨幅卡纵向堆叠，不允许把 Home 撑出横向滚动。

## Home AI 前沿

- AI 前沿沿用 `home-card-last30` / `wide-short` / `last30-column` 组件族，继续位于 `home-lane-primary`；本次只更新内容语义，不新增局部列或固定高度。
- 三栏数据语义固定为“最新发布 / 本周前沿 / 近 30 天关键进展”，但页面栏头只显示蓝色时间范围“最近 3 天 / 本周 / 近 30 天”；不再重复显示黑色栏目名和栏目简介。新闻卡片标题与摘要使用中文；品牌名、模型名、API、Agent、MCP 等专有名词可以保留英文。
- 新闻卡只展示中文事实标题、具体变化摘要、日期和来源。不要展示 `active`、自动归类、来源较稳、自动观察或“建议关注”一类内部状态。
- 同一新闻不能跨三栏重复；窄屏下三栏纵向堆叠，不允许长模型名、日期或来源把页面撑出横向滚动。

## Blog 对齐

- Blog 的 sidebar、品牌区、卡片圆角、hover 和图标容器要和 Dash 对齐。
- Blog 是公开笔记发布层，仍然保持工具型、归档型气质，不做营销 landing。
- 文章卡、专题卡、总览卡都使用同一圆角和悬浮规则。
- Blog 顶部入口和 Dash 顶部入口保持弱按钮样式，不抢主内容注意力。

## 登录页

- 登录页是 Dash 的访问入口，不属于 Home 导航页；沿用 Dash 的浅蓝灰背景、白卡片、`14px` 圆角、淡边框、轻阴影和系统字体。
- 桌面端使用单张双栏卡：左侧承载品牌和四个紧凑状态信号，右侧只放登录表单；移动端在 `760px` 以下改为单栏，不允许横向溢出。
- 登录页不是营销 landing：不使用巨大 hero、装饰插画、外部字体或重渐变。状态信号复用蓝、紫、青、绿语义色和现有小图标语言。
- 输入框使用淡边框和浅蓝 focus ring；主按钮使用 `var(--accent-strong)`，hover 仅上浮 `2px`，不改变布局尺寸。
- 错误信息使用红色语义，密码显示切换必须有可访问名称；登录态由服务端 HttpOnly Cookie 管理，页面脚本不得读取、保存或拼接密码。

## UI 质检清单

### Dash 全局微调基线

- Dash 的七个主页面与登录入口共用同一套基础字形：优先使用系统内的 `Segoe UI Variable Text`，继续保留中文系统字体回退；所有日期、时间、百分比、流量、Token、金额和课次统一使用等宽数字，避免数据刷新时视觉跳动。
- 外层 `panel`、页面页头和指标卡继续保持白色 / 极浅语义渐变，不增加彩色顶条；阴影使用两层轻阴影，只表达层级，不制造悬浮面板感。内部小项可以更扁平，但边框、圆角和背景必须与所属组件族一致。
- 侧栏入口、顶部弱按钮、范围 tab、文本按钮、图标按钮与页面内可操作卡必须有键盘可见的 focus ring；鼠标按下只允许轻微下沉，不改变布局尺寸。系统开启 reduced motion 时，动画和过渡应近乎即时完成。
- 无数据不再只显示一行游离文字：通用空状态使用浅灰蓝底、淡虚线边框和居中说明；图表空状态保留足够高度，让“暂无数据”和真实图表占据同一信息位置，但不能伪造趋势或占位数值。
- 生活页结果台桌面高度使用 `300px`，窄屏使用 `240px`；未抽取时的灰色提示降低字号，真实抽取结果仍保持高视觉权重。候选未选中时降低权重但必须保持可读，数量步进器与主操作均需有 hover / focus / pressed 反馈。
- `860px` 及以下侧栏进入页面顶部流式布局，七个一级入口保持两列，包括 `390px` 手机宽度；不要退成占据整屏的单列导航，也不要引入折叠菜单。
- 全局微调只统一视觉层级、密度和交互反馈，不改变页面信息架构、数据语义或页面专属强调色。任何局部新样式都应先尝试继承这套基础规则。

- 新增或移动 UI 卡片 / widget 时，必须先列出它要继承的组件族规则：圆角、边框、阴影、hover / focus、transition、语义色、响应式约束和缓存版本。
- 新增或移动 Home 卡片时，必须先写清楚它属于 `home-lane-primary` / `home-lane-signal` / `home-lane-rail` 哪一条语义 lane，以及它的 `data-card-size`；内容型长模块优先进入 `home-lane-primary` 填充左列，短扫读状态入口才进入右侧 `home-side-stack`。视觉列只允许由 `home-board` 和 `home-side-stack` 决定，右列宽度用 `widget-compact` / `widget-wide` 等卡型维护，顺序用卡片级 `order` 维护，并确认没有引入新的局部嵌套列、固定高度补白或跨行 row-height 牵连。
- 给新 class 命名后，用 `rg` 查一次它是否进入对应规则组，例如 `rg -n "summary-weather|summary-clock|hover|transition|translateY|height|grid-template" dash/styles.css STYLE_CONTEXT.md`。
- 同一行并列卡片不能只看宽度对齐；需要验证 `top`、`bottom`、`height` 都一致，尤其是 Home 顶部主卡 + 右侧 widget 这种跨 grid 区域。
- 间距和图标优先按 4px 网格检查；图标容器、文字 baseline、卡片内边距要上下左右对齐，不用肉眼凑位置。
- 图标尺寸、留白和颜色要成组一致；同一级入口不要混用明显不同粗细、填充方式或视觉重心的图标。
- 颜色只沉淀少量可复用变量：背景、主文本、弱文本、边框、主强调、危险状态；不要为了趋势扩成复杂 design token 系统。
- 配色以白底可读性和按钮 / 状态对比度为准；新增颜色前先确认它承担了新的语义，而不是只为了装饰。
- 实现芭蕾模块时单独检查粉玫瑰正文 / 数值的白底对比度、浅粉只作底色、失败红与候补橙没有混淆。
- 字体层级保持少量稳定档位：页面标题、区块标题、正文、弱说明、数字指标；不要在局部随手新增相近字号。
- 微动效只用于 hover / focus / tab 切换等轻量反馈；没有跨页面复用前，不单独抽象动效 token。
- 导出或新增视觉资产时，同步检查文件格式、命名、尺寸、透明背景、暗浅底可读性，以及引用路径是否和 Dash / Blog 对齐。
- 正式标识方向单独讨论，不跟随年度 logo 趋势临时改；MaxNow 标识应服务“个人状态工作站”的长期识别。

## 修改样式时的检查

- 豆奶详情页顶部三块 tab 当前为：今日签到、账号余量、签到累计；今日签到 tab 展示今日流量、今日豆丁、今日延长，使用和 Home 豆奶摘要一致的白底小指标卡结构。
- 改 Dash 样式时提升 `dash/index.html` 中的 `styles.css?v=...`。
- 改 Dash 脚本渲染时提升 `dash/index.html` 中的 `app.js?v=...`。
- 改 Blog 样式时提升所有 Blog 页面里的 `styles.css?v=...`。
- 改 Blog Preview 样式时提升 `preview.css?v=...`。
- 完成后运行 `python scripts/check.py`。
- 对 UI 改动至少验证：
  - 首页没有横向溢出。
  - 需要同高的同排卡片满足同顶、同底、等高。
  - 新增或移动的卡片 class 已覆盖 hover / focus selector，且 hover 不改变布局尺寸。
  - 豆奶页没有横向溢出。
  - 同行记页面没有横向溢出，地图点位标签不会挤出地图容器。
  - 芭蕾入口上线时，侧栏在 `720px` / `768px` 高度以及 `860px` / `390px` 宽度下可滚动、无孤立错位；芭蕾页头、指标、历史和图表没有整页横向溢出。
  - Blog 首页没有横向溢出。
  - 长中文、日期、`d/h` 单位不会挤出小卡。
