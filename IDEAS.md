# MaxNow 想法记录

这个文件用来长期保存 MaxNow 的产品想法、未来入口、暂时搁置的方向和待研究问题，避免每次聊完之后想法散掉。

## 使用规则

- 新想法先记在这里，之后再判断要不要进入实现。
- 可以保留原始想法，也可以补一段简短解释。
- 每个想法标记状态：`active`、`parked`、`research`、`done`。
- 这里只代表“已记录的想法”，不等于已经进入当前版本范围；真正确定要做的内容再同步进 `SPEC.md`。

## 正在研究

### 可信状态工作站

- 日期：2026-07-10
- 状态：active
- 来源：MaxNow 整体体检

MaxNow 下一阶段不以增加更多一级页面为目标，而是让现有私人状态工作站变得更可信、更安全、更容易长期维护。

核心方向：

- 私人默认：Dash 中的待办、关系记录、Token 和服务器状态默认只对 Owner 开放；Blog 继续承担公开内容。
- 状态可信：每个关键数字都能说明来源、最后成功时间和异常状态，不把读取失败伪装成 0 或“暂无数据”。
- 判断不过期：Home 的当前主线和待推进应与 ROADMAP、已完成功能和 Owner 判断一致。
- 自动化可观察：定时任务除了写入数据，还要能暴露失败、连续异常和恢复情况。
- 变更可验证：页面布局、数据契约、主要交互、移动端和无障碍状态进入自动检查，不再只靠人工扫一眼。
- 依赖可控：逐步收紧第三方脚本、地图资源、外链协议和浏览器安全策略。

执行优先级：

1. 先保护 `dash.maxnow.cn` 的访问。
2. Home 项目状态可信度已完成，下一步处理数据失败提示和新鲜度闭环。
3. 补自动测试、移动端和无障碍闭环。
4. 芭蕾已成为第七个一级入口；除此之外的新页面或桌面伴随入口仍需重新评估。

### MaxNow 个人博客

- 日期：2026-06-17
- 状态：active
- 来源：Owner 想法

给 MaxNow 增加公开博客，但不把博客混进私人状态工作站。

推荐域名结构：

- `dash.maxnow.cn`：私人状态工作站，继续保持内部、紧凑、偏操作型。
- `blog.maxnow.cn`：公开博客，用于发布筛选后的旧文和未来文章。
- `maxnow.cn`：未来公开主页或个人入口，后续链接到博客和项目。

内容源：

- private personal-wiki 的 `raw/blog-vioiv` 保存旧博客原始归档。
- 当前已归档 211 篇 Hexo Markdown 和 167 个缓存图片。
- personal-wiki 负责原文、筛选、隐私判断和长期知识归档。
- MaxNow 负责发布层、公开页面、静态构建和部署状态入口。

第一阶段方向：

- 做静态博客，不引入登录、数据库或后端 API。
- 只发布明确标记为 public / published 的文章。
- 将 Hexo front matter 转成 MaxNow 博客模型：标题、日期、分类、标签、slug、来源文件、封面/图片。
- `dash.maxnow.cn` 只显示博客发布状态和跳转入口，不承载完整阅读体验。

待判断问题：

- 第一批公开文章从哪些分类开始：CS 基础、算法模板、个人总结，还是先做精选。
- 发布标记写在 personal-wiki 原文 front matter，还是由 MaxNow 维护独立 manifest。
- 旧文是否保留原始日期和语气，还是发布前逐篇加编辑说明。
- 是否需要站内搜索、RSS、访问统计、评论或邮件订阅。

### 芭蕾学习模块

- 日期：2026-07-26
- 状态：implementing
- 来源：Owner 想法与 LIJUN / 闻道约课实测

给 MaxNow 增加一个长期的个人芭蕾学习模块。它不只是“周日抢课”，而是把课程计划、真实上课、学习进展和受控自动化放在同一个连续上下文里。

最值得先补的内容：

- 当前学习级别、每周目标节数和近期重点课程。
- 下一节课、未来已约课程、候补状态与取消截止时间。
- 与预约记录分开的真实上课记录，避免“约过”被误当成“上过”。
- 累计节数与小时、当月 / 当年趋势，以及芭蕾 / 软开 / 肌肉素质 / 技巧等课型和 L1、L1.5、L2、L3、L4 等级别分布。
- 每节课的重点、老师纠正、动作标签和下次要注意的事项。
- 可选的疲劳或身体状态备注；它属于敏感个人信息，默认不进 Home。
- Session、新鲜度和最近同步结果，让旧课表不会被误认成实时状态。

页面方向：

- 芭蕾信息深度已经超过“生活 / 吃啥”轻工具，已作为独立页面实现，标题“芭蕾”、副标题“课程与进度”，导航位于 Token 与云服务之间。
- 页面沿用现有 `secondary-view` 视觉协议，使用区别于同行记亮粉的粉玫瑰 + 白卡语义；第一屏优先显示下一节课、累计节数 / 小时、本月和本周指标，再展示未来课程、上课记录、趋势、课程分布和笔记。
- Home 使用 `widget-wide` 紧凑入口，仅显示下一节课、本周进度和状态，不复制完整周课表。
- 折线图用“月 / 年 / 全部”切换时间范围，用“节数 / 小时”切换单一指标；课程细分使用水平条或排行，不使用双轴折线或难比较的小饼图。
- 自动化运维信息留在 Cloud；芭蕾页只显示 Owner 能直接理解的粗状态和最近结果。

数据方向：

- 闻道负责课程、预约、候补、上课记录和会员课次等机器事实。
- personal-wiki 负责学习目标、课堂笔记、练习记录和人工判断。
- MaxNow 负责把两类来源合成脱敏只读缓存；前端不直连闻道，也不回写 personal-wiki。
- 上课事实使用服务器本地单一 JSON 账本：首次全量、每天重扫最近 60 个逻辑日并 upsert、每月 1 日 00:47 全量校验；同步成功才原子替换，失败保留最后成功缓存。
- 每天以北京时间 00:00 为逻辑日界，生产目标在 00:17 运行，避开现有午夜任务并等待前一日记录入账。独立状态文件记录最近尝试、最近成功、失败原因、抓取窗口与合并数量；当前预约使用独立快照和 36 小时 TTL。
- 00:17 是生产目标时刻，不代表任务已启用。后续若再次运行固定间隔的 `PHPSESSID` 活跃寿命 / 滑动窗口实验，生产抓取仍应保持待启用，避免额外 GET 改变实验频率和结论；当前三阶段实验已在首个标准 25 分钟样本确认身份失效后停止。
- 生产凭据使用服务器 host-bound systemd 加密凭据与 `LoadCredentialEncrypted`；`PHPSESSID` 不进入 Git、日志、前端、聊天、环境变量或命令参数。身份失败后保留旧缓存并暂停重试，直到凭据版本变化；Owner 在电脑微信重新登录后再安全刷新凭据。
- 月度、年度和全局统计在同步时预聚合；浏览器切换范围时直接读取，不反复扫描整份历史。课程归类规则版本化，后续改规则可从本地账本重算。
- 人类可读芭蕾档案如果以后需要，只从 canonical ledger 单向生成；不维护第二份可手改的上课事实文档。
- 课程余位只是抓取时快照；任何统计都要保留来源更新时间、缺失和过期状态。

分层能力：

1. 只读学习看板：下一节课、本周训练、未来预约、上课记录和学习统计。
2. 学习闭环：目标、课堂笔记、练习计划和阶段复盘。
3. 约课 dry-run：根据课程、老师、时段、优先级、候补和冲突规则生成预计动作，不提交。
4. 受控自动约课：只有 Session 实验和连续 dry-run 通过、Owner 单独批准后才启用。

待判断问题：

- 当前学习级别、每周目标节数和主要训练方向分别是什么？
- 学习目标和课后笔记是否统一维护在 personal-wiki，采用 Markdown 还是结构化 JSON？
- 可约课程是否需要完整周视图，还是只展示符合偏好的候选？
- “肌肉素质”“技术技巧”等课程是否要从“其他”继续拆成独立类别？
- 预约、候补、登录失效和未知错误通过噗噗、微信还是 MaxNow 状态卡通知？
- 身体状态是否需要单独记录；如果需要，保留到什么粒度、多久后自动隐藏？

### Last-30 滚动记忆

- 日期：2026-06-14
- 状态：research
- 来源：Owner 想法

给 MaxNow 增加一层“最近 30 天”的滚动上下文，让它每天自动维护今天、本周和近 30 天的大事。

目标：

- 每天保留今日大事，而不是只看当下状态。
- 每周沉淀本周推进、完成、变化和卡点。
- 最近 30 天保留持续主线、重要决定和等待项。
- 让 MaxNow 逐渐从看板变成个人连续记忆。

初步方向：

- 单独维护 `data/last-30.json` 和 `data/last-30.js`。
- 单独维护 `openclaw/last-30/SKILL.md`。
- Home 可以先显示紧凑摘要，完整详情以后再决定是否需要新页面。
- 自动更新由服务器上的执行器触发，优先复用 OpenClaw 的定时任务边界。

待判断问题：

- Last-30 是否独立成数据文件，还是先嵌入 `data/dashboard.json`？
- 每天新增事实来自 Git、OpenClaw 运行结果、Token 统计、手动记录，还是以后接更多来源？
- 哪些判断必须标记为 `needsOwnerConfirm`，避免自动化替 Owner 下结论？

### 桌面伴随面板

- 日期：2026-06-12
- 状态：research
- 来源：Owner 想法

给 MaxNow 增加桌面级入口，让个人状态工作站不需要每次打开浏览器也能被看到。

macOS 方向：

- 做一个顶部状态栏里的小应用。
- 点击状态栏图标后，下拉一个紧凑的个人面板。
- 面板显示今日状态、当前主线、今日推进、关键时间点和简洁的系统状态。
- 气质应该像私人控制台，不像通知流。

Windows 方向：

- 做一个桌面壁纸式个人看板。
- 它更像覆盖在桌面上的平静状态层，而不是普通应用窗口。
- 显示和网页看板相同的核心状态信息，但适配常驻、扫一眼就能懂的场景。
- 不要变成完整 todo 工具，也不要变成新闻墙。

共同原则：

- 浏览器里的 `dash.maxnow.cn` 仍然是 v1 的标准入口。
- 桌面入口尽量复用同一套数据契约。
- 个人状态优先，外部 AI 输入保持次要。
- 交互保持紧凑、安静、偏操作型。

待判断问题：

- 桌面伴随面板应该是独立应用、静态网页外壳，还是直接读取 `data/dashboard.json` 的小原生应用？
- Windows 版本应该是真壁纸、透明置底窗口，还是桌面 widget 层？
- macOS 和 Windows 后续要共用一个跨平台框架，还是分别用平台原生方案？

## 暂时搁置

### MaxNow 公开主页

- 日期：2026-05-25
- 状态：parked
- 来源：早期产品方向

先把 `maxnow.cn` 留给未来公开主页或个人入口。当前私人看板放在 `dash.maxnow.cn`，公开博客优先使用 `blog.maxnow.cn`。

### 灵感宇宙

- 日期：2026-05-25
- 状态：parked
- 来源：早期产品方向

保留“想法、灵感、人生感悟像星点一样漂浮在个人宇宙里”的方向。它未来可以成为公开或半公开的 MaxNow 体验，但和当前私人操作型看板分开。

## 已落入规格

### 私人状态工作站

- 日期：2026-05-26
- 状态：done
- 来源：当前规格

MaxNow v1 已定义为 `dash.maxnow.cn` 上的私人状态工作站，重点是当前状态、主线、今日推进、日常记录、系统状态和 Token 使用情况。
