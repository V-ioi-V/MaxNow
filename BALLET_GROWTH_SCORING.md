# 芭蕾成长进度计算标准

本文档单独记录 MaxNow 芭蕾“成长进度”卡的计算标准，是该机制的唯一人类可读规则文档。

- 课程路径、成人学习建议课次和阶段效果语义来源：private personal-wiki 的 `raw/relationship-ricky/docs/2026-07-20-lijun-ballet-course-guide.md`。
- 升班课次只表达已经完成的课程数量；XP 权重和等级阈值是 MaxNow 的个人游戏化参数，不是机构评分、健康评价或老师考核结论。
- 执行代码位于 `dash/app.js`：`BALLET_PROMOTION_RULES`、`BALLET_XP_BY_COURSE_TYPE`、`BALLET_XP_LEVELS`、`getBalletPromotionState()` 和 `getBalletXpState()`。
- 页面显示的 `2 / 15 节`、`150 / 300 XP` 等数字是根据上课记录实时计算的结果，不是固定规则，不写死在本文档中。

<!-- BALLET_GROWTH_CONTRACT_START -->
## 一、参与计算的数据

### 1. 有效上课记录

数据来自脱敏的 `balletData.records`。以下状态不参与升班课次和 XP：

- `cancelled`
- `canceled`
- `absent`
- `no_show`
- `已取消`
- `未到课`

其他实际上课记录视为有效记录。同一节课只计算一次。

### 2. 当前级别

只从 `courseType = ballet` 的有效芭蕾基训记录中判断级别。

级别顺序固定为：

`L1 → L1.5 → L2 → L3 → L4 → L5`

当前级别取有效芭蕾基训记录中已经出现的最高级别；尚无可识别级别时从 L1 开始。升班课次只统计当前级别的芭蕾基训，不把其他级别、软开、肌肉素质或技术技巧混入课次。

## 二、规律与间歇口径

计算以数据截至日 `dataAsOf` 或最近成功同步时间为锚点。

1. 从当前级别第一节有效课程到数据截至日计算观察天数，最多取最近 28 天。
2. 观察天数至少覆盖 21 天，才具备判断规律性的样本。
3. 在最近最多 28 天内计算：

`平均每周课次 = 最近有效课次 ÷ 观察天数 × 7`

4. 同时满足“至少覆盖 21 天”和“平均每周至少 2 节”，使用规律课次标准；否则使用间歇课次标准。

## 三、升班目标课次

| 当前级别 | 下一等级 | 规律课次 | 间歇课次 |
| --- | --- | ---: | ---: |
| L1 | L1.5 | 8 | 15 |
| L1.5 | L2 | 15 | 25 |
| L2 | L3 | 20 | 30 |
| L3 | L4 | 30 | 40 |
| L4 | L5 | 30 | 40 |
| L5 | — | 30 | 40 |

页面只显示当前级别已经完成的芭蕾基训课次与当前采用的目标课次，不加入基础月份，也不换算为 100 分。L5 是当前指南最高级别，不再计算下一等级，页面只显示 L5 的累计课次。

## 四、升班课次进度

`升班课次进度 = min(当前级别有效课次 ÷ 当前采用的目标课次, 1) × 100%`

百分比只用于进度条宽度，页面主数字始终显示：

`当前级别有效课次 / 当前采用的目标课次`

超过目标的课次不会把进度条画出容器，但已经完成的真实课次数量可以继续保留。达到目标课次只能显示“可咨询老师进行下一等级测评”，不能直接判定已经升班、具备升班资格或已经掌握对应能力。实际升班以老师测评和 Owner 的人工确认结果为准。

## 五、XP 计算

全部有效上课记录参与 XP。每节课根据归一化后的 `courseType` 只采用一个权重：

| courseType | 课程含义 | 每节 XP |
| --- | --- | ---: |
| ballet | 芭蕾基训 | 100 |
| technique | 技术技巧 | 80 |
| other | 其他 / 不定期课程 | 70 |
| conditioning | 肌肉素质兼容键 | 60 |
| muscle | 肌肉素质 | 60 |
| soft_open | 软开 | 50 |
| flexibility | 软开兼容键 | 50 |

`累计 XP = 所有有效上课记录对应 XP 之和`

无法识别的课程类型按 `other = 70 XP` 处理。

## 六、经验等级与效果

| 等级 | 起始 XP | 名称 | 当前效果 |
| ---: | ---: | --- | --- |
| 1 | 0 | 发力启蒙 | 正确发力方向 |
| 2 | 300 | 单一元素 | 动作标准 +1 |
| 3 | 800 | 枢纽建立 | 重心与骨盆 +1 |
| 4 | 1500 | 协调开范 | 四肢协调 +1 |
| 5 | 2500 | 组合流畅 | 舒展与标准 +1 |
| 6 | 4000 | 认知串联 | 控制与完整 +1 |

当前等级取累计 XP 已达到的最高起始阈值。尚未到最高等级时，卡片显示：

`累计 XP / 下一等级起始 XP`

等级进度条采用：

`(累计 XP - 当前等级起始 XP) ÷ (下一等级起始 XP - 当前等级起始 XP)`

六级名称和效果来自课程指南的课程定位语义，但“+1”和 XP 等级属于 MaxNow 内部的游戏化表达，不表示真实技术等级或身体能力测量。

## 七、修改与同步要求

任何以下变更都必须在同一分支同步完成：

1. 修改 `dash/app.js` 中的升班课次或 XP 执行代码。
2. 修改本文档中的对应公式、参数表或解释。
3. 在 `UPDATE_LOG.md` 记录变更原因和新旧口径。
4. 运行 `python scripts/check.py`。
5. 按项目规则更新 `VERSION` 和 `dash/data/project-meta.*`。

`scripts/check.py` 会把本文档中的升班参数表、XP 权重表和经验等级表与 `dash/app.js` 的常量直接比较。只修改代码或只修改本文档，检查都必须失败。
<!-- BALLET_GROWTH_CONTRACT_END -->
