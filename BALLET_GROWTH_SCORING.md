# 芭蕾成长进度计算标准

本文档单独记录 MaxNow 芭蕾“成长进度”卡的计算标准，是该机制的唯一人类可读规则文档。

- 课程路径和成人学习建议课次来源：private personal-wiki 的 `raw/relationship-ricky/docs/2026-07-20-lijun-ballet-course-guide.md`。
- 升班进度与成长等级都只使用实际上课数量，不换算分数或 XP。
- 执行代码位于 `dash/app.js`：`BALLET_PROMOTION_RULES`、`BALLET_GROWTH_LEVELS`、`getBalletPromotionState()` 和 `getBalletGrowthLevelState()`。
- 页面上的课次由同步记录实时计算，不在本文档中写死当前结果。

<!-- BALLET_GROWTH_CONTRACT_START -->
## 一、参与计算的数据

数据来自脱敏的 `balletData.records`。以下状态不参与升班进度和成长等级：

- `cancelled`
- `canceled`
- `absent`
- `no_show`
- `已取消`
- `未到课`

其他实际上课记录视为有效记录。同一节课只计算一次。

## 二、课程级别与自动升班

升班只使用 `courseType = ballet` 的有效芭蕾基训记录。课程级别顺序固定为：

`L1 → L1.5 → L2 → L3 → L4 → L5`

当前级别先取有效芭蕾基训记录中已经出现的最高级别；尚无可识别级别时从 L1 开始。只统计当前级别的基训课次，不把其他级别、软开、肌肉素质或技术技巧混入。

当前级别课次达到正在采用的目标后，页面自动进入下一课程级别，并从下一课程级别的实际上课记录重新累计。若多个连续级别都已达到目标，可连续自动升级。L5 是当前课程路径最高级别。

## 三、规律与间歇口径

计算以数据截至日 `dataAsOf` 或最近成功同步时间为锚点。

1. 从当前级别第一节有效课程到数据截至日计算观察天数，最多取最近 28 天。
2. 观察天数至少覆盖 21 天，才具备判断规律性的样本。
3. 在最近最多 28 天内计算：

`平均每周课次 = 最近有效课次 ÷ 观察天数 × 7`

4. 同时满足“至少覆盖 21 天”和“平均每周至少 2 节”，使用规律课次标准；否则使用间歇课次标准。
5. 某一级别一旦曾按规律口径达到规律课次目标，就永久保留该次升级结果；之后训练频率下降或暂停，不得把已经自动进入的等级回退。

## 四、升班目标课次

| 当前级别 | 下一等级 | 规律课次 | 间歇课次 |
| --- | --- | ---: | ---: |
| L1 | L1.5 | 8 | 15 |
| L1.5 | L2 | 15 | 25 |
| L2 | L3 | 20 | 30 |
| L3 | L4 | 30 | 40 |
| L4 | L5 | 30 | 40 |
| L5 | — | 30 | 40 |

页面显示“当前级别实际上课次 / 当前采用目标课次”，不加入月份，也不换算成 100 分。

`升班进度 = min(当前级别有效课次 ÷ 当前采用目标课次, 1) × 100%`

达到目标课次后自动进入下一等级，不再等待测评或人工确认。这里的“自动升级”只改变 MaxNow 的课程进度显示，不代表机构学籍或外部系统被修改。

## 五、十级成长等级

成长等级统计所有课程类型的有效上课记录，每节课固定计 1 节，不区分芭蕾基训、软开、肌肉素质或技术技巧。

| 等级 | 累计课次门槛 |
| ---: | ---: |
| 1 | 0 |
| 2 | 10 |
| 3 | 25 |
| 4 | 45 |
| 5 | 70 |
| 6 | 95 |
| 7 | 120 |
| 8 | 145 |
| 9 | 170 |
| 10 | 200 |

当前等级取累计有效课次已经达到的最高门槛。达到下一门槛时自动升级；累计 200 节时进入 Lv.10，Lv.10 是满级。

页面标题只显示当前成长等级 `Lv.N`，不铺开全部十级，也不在标题重复下一等级。主数字显示“本级已上课次 / 本级目标课次”，同时显示“还差 N 节升级到 Lv.M”；这样只回答当前等级和距离下一等级还有多远。Lv.10 显示“Lv.10 / 已满级”，超过 200 节后真实累计节数继续保留。

## 六、小天鹅成长图像

成长卡使用十张本地透明 PNG 模拟同一只小天鹅的自然成长过程：

- Lv.1–Lv.3：灰色绒毛雏鹅，身体与颈部逐步变长。
- Lv.4–Lv.6：灰白换羽阶段，幼绒逐渐过渡为完整羽毛。
- Lv.7–Lv.9：基本变为白色，颈线、身体比例与折叠翅膀逐步成熟。
- Lv.10：白色成年天鹅，并使用克制的光环与星点表示满级。

确认的源图保存在 `dash/assets/ballet/swan-growth-sheet.png`，页面实际加载 `swan-lv1.png`–`swan-lv10.png` 十张透明裁切图。前端只按当前等级选择对应素材，不在运行时生成或改变姿态。图像只提供 Lv.1–Lv.10 的视觉反馈，不增加额外名称、技术效果或评分含义。

## 七、修改与同步要求

任何以下变更都必须在同一分支同步完成：

1. 修改 `dash/app.js` 中的自动升班或成长等级执行代码。
2. 修改本文档中的对应公式、参数表或解释。
3. 在 `UPDATE_LOG.md` 记录变更原因和新旧口径。
4. 运行 `python scripts/check.py`。
5. 按项目规则更新 `VERSION` 和 `dash/data/project-meta.*`。

`scripts/check.py` 会把本文档中的升班参数表和十级课次门槛与 `dash/app.js` 的常量直接比较。只修改代码或只修改本文档，检查都必须失败。
<!-- BALLET_GROWTH_CONTRACT_END -->
